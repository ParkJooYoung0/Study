const arr = ['가','라','다','라','마','라'];
undefined
arr.indexOf('라')
1
arr.splice(1, 1)
['라']
arr.indexOf('라')
2
arr.splice(2, 1)
['라']
arr.indexOf('라')
3
arr.splice(3, 1)
['라']
arr
(3) ['가', '다', '마']
arr.indexOf('라')
-1
while(arr.indexOf('라') > -1) {
    arr.splice(arr.indexOf('라'),1)
}

['라']
arr
(3) ['가', '다', '마']
let index = arr.indexOf('라');
while (index > -1) {
    arr.splice(index, 1);
    index = arr.indexOf('라');
}
undefined
arr
(3) ['가', '다', '마']
